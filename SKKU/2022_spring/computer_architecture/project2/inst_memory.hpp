#ifndef INST_MEMORY_HPP
#define INST_MEMORY_HPP

#include <vector>

#include "memory.hpp"

class InstMemory : public Memory {
    public:

    InstMemory();
    
    //InstMemory(
    //    uint32_t outer_addr_ofst,
    //    uint32_t addr_space_size,
    //    std::vector<uint32_t> instruction
    //);


};

#endif